# UI Enhancement TODO

- [ ] Review current UI classes/structure in `frontend/src/App.jsx` and styling in `frontend/src/styles.css`.
- [ ] Update `frontend/src/styles.css` with richer backgrounds:
  - [ ] Add subtle global gradient/noise layer
  - [ ] Enhance `.auth-shell`, `.topbar`, `.event-hero`, `.seat-grid` backgrounds
  - [ ] Improve shadows/elevation and card/button styles
  - [ ] Polish `.alert` and `.receipt`
  - [ ] Add `:focus-visible` and reduced-motion fallbacks
- [x] Run/build frontend to ensure no CSS errors.
- [ ] Sanity-check responsive breakpoints.

